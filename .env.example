# Weather API Integration
# Get from: https://openweathermap.org/api

# OpenWeatherMap API Key for weather data
OPENWEATHER_API_KEY=your_openweathermap_api_key_here

# Optional explicit CORS allowlist for cross-origin API calls
# If unset, the app auto-allows Netlify runtime URLs (URL/DEPLOY_PRIME_URL)
# plus localhost dev origins.
# Example:
# CORS_ALLOWED_ORIGINS=https://myaioutfit.com,https://preview.example.com
CORS_ALLOWED_ORIGINS=

# OpenAI API Key for local wardrobe image generation script
# Used by: npm run generate:wardrobe-images
OPENAI_API_KEY=your_openai_api_key_here

# TinyPNG API key for optional image compression script
# Used by: npm run compress:wardrobe-images -- --provider tinypng
TINIFY_API_KEY=your_tinify_api_key_here

# Replicate API key for background removal (used in Supabase Edge Functions)
# Get from: https://replicate.com/account/api-tokens
# Note: This key is used server-side only in Supabase Edge Functions
# Set via: supabase secrets set REPLICATE_API_TOKEN=<your-token>
REPLICATE_API_TOKEN=r8_your_replicate_api_token_here

# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder value with your actual API key
# 3. Get your free API key from https://openweathermap.org/api
# 4. Never commit .env.local to version control

# For Netlify deployment:
# Set this environment variable in your Netlify dashboard under Site Settings > Environment Variables
